/*
duas funçoes:
 adicionar:
1)recuperar oq estiver escrito no campo

2)verificar se está vazio ou preenchido
    if (vazio) {
         escrever para informar valido
    if (preenchido) {
     verificar se ja existe o objeto no array
            if (existir) {
            escrever objeto ja adicionado
     }
            if (nao existe) {
            incluir valor preenchido no campo para o array
            efetuar consologlog no array para debug
            limpar o valor contido no campo de entrada do texto
      }

ordenar:
1)ordenar de forma allfabetica o array objetos
2)efetuar console.log para debugar



*/